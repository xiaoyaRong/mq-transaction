<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sx.dao.PaymentDao">

    <insert id="insertOne" parameterType="com.sx.entity.PrePaymentEntity">

        insert into prePayment
        values (null,
                #{record.amount},
                #{record.payStatus},
                #{record.parkingId}
               )
    </insert>

</mapper>